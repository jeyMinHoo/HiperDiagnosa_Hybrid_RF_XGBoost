{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -m gunicorn --workers 2 --bind 0.0.0.0:$PORT app:app",
    "healthcheckPath": "/api/metrics",
    "healthcheckTimeout": 30
  }
}